list_name.append("anything")#to add to list------------------------------------------------ADD
list_name.insert(1, "an3ything")#to add on a specific index--------------------------------PUT IN
list_name.remove("anything")#--------------------------------------------------------------REMOVE
list_name.pop(2)#remove anything that was on third [position]------------------------------DELETE INDEX
list.sort() # Output: [1, 2, 4, 9]---------------------------------------------------------SORT NUMBERS ASCENDING
list.reverse()  # Output: [9, 4, 2, 1]-----------------------------------------------------SORT NUMBERS DESCENDING
squares=[x**2 for x in range(1, 6)]#formula with varing value- Output: [1, 4, 9,16]--------TRY NUMBERS IN FORMULA
if "apple" in fruits:#--------------------------------------------------------------------LOOKS FOR A STRING IN LIST
unpaired ^= number
 if A.count(number) == 1:
print(format(dj,".2f"))
s=random.randint(1,999)
def hello(to="World."):#to hard code hallo to "to" if nothing is given
name=name.strip()#to remove spaces from string
name=name.capitalize()#to capitalize first alphabet
name=name.title()#to capatilize first letter of every word
first, last =name.split(" ")#splits one variable in two 
pow(n, .5)#n power 5
from  file import function
print(f"{z:,}")#to add , after 3 digits of number
print(f"{z:.2f}")#goes upto two decimal points 
c[::-1]#to print it oppsitely
math .floor .ceil .pi .radians .sin .cos

['and continue finally'] ['is '],raise
['as'] '[def] [for] 'lambda ['return']
assert '[del] [from]'[' None'] '[True]
'async '[elif global]' nonlocal '[try]
'await' [else if not while]
[break] [except import or] [with]
[class] [False] [in]'[' pass'], yield

dict.fromkeys(my_list)

import matplotlib.pyplot as plt
sizes = [25, 30, 35, 10]
plt.barchart(sizes)
plt.show()
import os
	os.remove("coffee.txt")
	os.rename("temp.txt", "coffee.txt")
random.random()
random.randint(1, 5)
random.randrange(1, 10, 2)
random.shuffle([12, 34, 56, 60])
random.choice([1, 2, 3, 4, 5])
random.sample("abcdefghijklmnopqrstuvwxyz", 15)
combined_list=[*list1,*list2,*list3]
def get_time() -> str:

from itertools import compress
DAYS=['monday','tuesday','wednesday']
MASK=[1,0,1]
print(list(compress(DAYS,MASK)))
:=
big_number=10_000_000_000_000_000
456_378.234_567_890_987_65
print(f'{big_number:_}')#only for ints
from typing import self
{import numpy
a=numpy.array([[1,2],[3,4]])
b=numpy.array([[5,6],[7,8]])
print(a@b)}
value=10
print(-~value)
from datetime import datetime
print(10>>1)
print(20<<2)
type annotation
chainmap

from timeit import timeit
print(memo.get(5))#plus error handling