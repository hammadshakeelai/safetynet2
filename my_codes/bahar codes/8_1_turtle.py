import turtle # Graphics module
turtle.speed(1)
turtle.forward(100)
turtle.left(90)
turtle.forward(100) # Don't change direction
turtle.done()  # Keep the window open. In terminal don't need this command
